# DRAFT — majority vote across tuned drafts
# tune_high_dir: /home/user/synth/result/tune_high (14 files)
# tune_low_dir:  /home/user/synth/result/tune_low (4 files)
# Rule: pick most frequent top-1 model per (asset, frequency, regime)
#
version: '2.2'
fallback_chain:
  L1_timeout_ms: 600
  L2_model: garch_v2
  L3_model: garch_v4
updated_at: '2026-04-07T17:45:20Z'
source:
  method: majority_vote_top1_name_and_params
routing:
  BTC:
    high:
      bull:
        models:
        - name: regime_switching
          weight: 1.0
          params:
            lookback_days: 30
            vol_ratio: 1.5
      high_vol:
        models:
        - name: regime_switching
          weight: 1.0
          params:
            lookback_days: 14
            vol_ratio: 1.5
      ranging:
        models:
        - name: garch_v2
          weight: 1.0
          params:
            mean_model: Zero
            lookback_days: 5
            vol_multiplier: 1.0
    low:
      bull:
        models:
        - name: garch_v2
          weight: 1.0
          params:
            mean_model: Constant
            lookback_days: 60
            vol_multiplier: 0.9
      high_vol:
        models:
        - name: regime_switching
          weight: 1.0
          params:
            lookback_days: 60
            vol_ratio: 2.0
      ranging:
        models:
        - name: ensemble_garch_v2_v4
          weight: 1.0
          params:
            weights:
            - 0.5
            - 0.5
  ETH:
    high:
      bull:
        models:
        - name: egarch
          weight: 1.0
          params:
            p: 1
            q: 1
            lookback_days: 5
      high_vol:
        models:
        - name: garch_v4
          weight: 1.0
          params:
            p: 2
            q: 1
            lookback_days: 7
            momentum_weight: 0.4
            drift_decay: 0.9
      ranging:
        models:
        - name: garch_v4
          weight: 1.0
          params:
            p: 2
            q: 1
            lookback_days: 10
            momentum_weight: 0.4
            drift_decay: 0.9
    low:
      bull:
        models:
        - name: jump_diffusion
          weight: 1.0
          params:
            lookback_days: 30
            jump_vol_scale: 1.5
      high_vol:
        models:
        - name: regime_switching
          weight: 1.0
          params:
            lookback_days: 45
            vol_ratio: 2.0
      ranging:
        models:
        - name: egarch
          weight: 1.0
          params:
            p: 1
            q: 1
            lookback_days: 30
  XAU:
    high:
      trending:
        models:
        - name: garch_v4
          weight: 1.0
          params:
            p: 1
            q: 1
            lookback_days: 10
            momentum_weight: 0.6
            drift_decay: 0.98
      mean_reverting:
        models:
        - name: garch_v2
          weight: 1.0
          params:
            mean_model: Zero
            lookback_days: 20
            vol_multiplier: 1.2
    low:
      trending:
        models:
        - name: production_baseline
          weight: 1.0
          params: {}
      mean_reverting:
        models:
        - name: garch_v4
          weight: 1.0
          params:
            p: 1
            q: 1
            lookback_days: 20
            momentum_weight: 0.6
            drift_decay: 0.98
  SOL:
    high:
      bull:
        models:
        - name: garch_v4
          weight: 1.0
          params:
            p: 2
            q: 1
            lookback_days: 5
            momentum_weight: 0.4
            drift_decay: 0.9
      high_vol:
        models:
        - name: garch_v4
          weight: 1.0
          params:
            p: 1
            q: 1
            lookback_days: 7
            momentum_weight: 0.4
            drift_decay: 0.9
      ranging:
        models:
        - name: regime_switching
          weight: 1.0
          params:
            lookback_days: 30
            vol_ratio: 1.5
    low:
      bull:
        models:
        - name: garch_v4
          weight: 1.0
          params:
            p: 1
            q: 1
            lookback_days: 10
            momentum_weight: 0.4
            drift_decay: 0.95
      high_vol:
        models:
        - name: garch_v4
          weight: 1.0
          params:
            p: 1
            q: 1
            lookback_days: 10
            momentum_weight: 0.8
            drift_decay: 0.9
      ranging:
        models:
        - name: garch_v2
          weight: 1.0
          params:
            mean_model: Constant
            lookback_days: 60
            vol_multiplier: 0.9
  NVDAX:
    low:
      market_open:
        models:
        - name: garch_v2_2
          weight: 1.0
          params:
            lookback_days: 15.0
            vol_multiplier: 1.0
      overnight:
        models:
        - name: garch_v2_2
          weight: 1.0
          params:
            lookback_days: 25.0
            vol_multiplier: 1.1
      earnings:
        models:
        - name: ensemble_garch_v2_v4
          weight: 1.0
          params:
            weights:
            - 0.5
            - 0.5
      trending:
        models:
        - name: ensemble_garch_v2_v4
          weight: 1.0
          params:
            weights:
            - 0.5
            - 0.5
  TSLAX:
    low:
      market_open:
        models:
        - name: arima_equity
          weight: 1.0
          params:
            p: 1
            q: 1
            lookback_days: 30
            off_hours_vol_scale: 0.3
      overnight:
        models:
        - name: weekly_regime_switching
          weight: 1.0
          params:
            lookback_days: 90
            regime_lookback: 10
            trending_vol_mult: 1.2
      earnings:
        models:
        - name: weekly_regime_switching
          weight: 1.0
          params:
            lookback_days: 90
            regime_lookback: 10
            trending_vol_mult: 1.2
      trending:
        models:
        - name: garch_v4
          weight: 1.0
          params:
            lookback_days: 25
            momentum_weight: 0.45
            drift_decay: 0.9
  AAPLX:
    low:
      market_open:
        models:
        - name: ensemble_garch_v2_v4
          weight: 1.0
          params:
            weights:
            - 0.5
            - 0.5
      overnight:
        models:
        - name: ensemble_garch_v2_v4
          weight: 1.0
          params:
            weights:
            - 0.5
            - 0.5
      earnings:
        models:
        - name: ensemble_garch_v2_v4
          weight: 1.0
          params:
            weights:
            - 0.5
            - 0.5
      trending:
        models:
        - name: markov_garch_jump
          weight: 1.0
          params:
            nu: 4.0
            lookback_days: 15
  GOOGLX:
    low:
      market_open:
        models:
        - name: ensemble_garch_v2_v4
          weight: 1.0
          params:
            weights:
            - 0.5
            - 0.5
      overnight:
        models:
        - name: seasonal_stock
          weight: 1.0
          params:
            lookback_days: 45
      earnings:
        models:
        - name: garch_v2
          weight: 1.0
          params:
            mean_model: Zero
            lookback_days: 60
            vol_multiplier: 1.2
      trending:
        models:
        - name: regime_switching
          weight: 1.0
          params:
            regime_method: er
            lookback_days: 45
            trending_vol_mult: 1.45
            sideways_vol_mult: 0.85
  SPYX:
    low:
      market_open:
        models:
        - name: ensemble_garch_v2_v4
          weight: 1.0
          params:
            weights:
            - 0.5
            - 0.5
      overnight:
        models:
        - name: weekly_seasonal
          weight: 1.0
          params:
            lookback_days: 45
      earnings:
        models:
        - name: ensemble_garch_v2_v4
          weight: 1.0
          params:
            weights:
            - 0.5
            - 0.5
      trending:
        models:
        - name: ensemble_garch_v2_v4
          weight: 1.0
          params:
            weights:
            - 0.35
            - 0.65
